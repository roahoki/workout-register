<div class="space-y-4">
  <%= link_to "← #{@routine.name}", routine_path(@routine), class: "text-sm text-zinc-500 hover:text-zinc-300 transition-colors" %>

  <div class="bg-zinc-900 border border-zinc-800 rounded-2xl p-6">
    <h1 class="text-xl font-bold text-zinc-100 mb-6">Edit routine</h1>
    <%= render "form", routine: @routine %>
  </div>
</div>
